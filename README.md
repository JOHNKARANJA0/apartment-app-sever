## WELCOME TO THE APARTMENT APP SERVER

# SETUP
 *Run:*
 *pipenv --python /usr/bin/python*  to setup the environment
 *pipenv install* to install the dependancies
 *pipenv shell* to enter the python environment